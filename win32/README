To make this work:

- copy the script folder inside the folder containing the eblink binary
- Or set environment variable EB_SCRIPT_PATH to the script directory

If used for stm32 debugging, the stm32gdb.bat file uses a path search option and can be used right away.

Have fun.